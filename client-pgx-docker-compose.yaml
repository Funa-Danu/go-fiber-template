services:
  postgres:
    image: postgres:16-alpine
    container_name: go-fiber-template-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    command: ["postgres", "-c", "listen_addresses=*"]
